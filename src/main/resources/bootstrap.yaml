info:
  build:
    name: Rally Service
    description: ReportPortal Rally Integration
    version: '@version@'
    branch: '@branch@'

spring:
  profiles:
    ## Default one is still Eureka
    active: ${rp.profiles:eureka}
  application:
    name: rally
